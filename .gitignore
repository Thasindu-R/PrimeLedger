# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
coverage

# Environment — .env.example is the only one that belongs in the repo
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
data/*.db

# Backend — Gradle / Java. The wrapper (gradlew, gradle/wrapper/) is committed
# on purpose so the build is reproducible without a local Gradle install.
backend/.gradle/
backend/build/
backend/bin/
backend/out/
*.class
