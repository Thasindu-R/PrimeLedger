/**
 * User profile: display name, avatar, base currency, locale, theme and date
 * format. Keyed by the Supabase {@code auth.users.id}.
 *
 * <p>Proposal §6.3. Populated in Phase 3, replacing the frontend's
 * {@code useProfile} localStorage hook.
 */
package com.primeledger.profile;
