/**
 * Application configuration: {@code SecurityConfig}, {@code CorsConfig},
 * {@code OpenApiConfig} and {@code SchedulerConfig}.
 *
 * <p>Proposal §6.3. Populated in Phase 2, extended in Phase 3 once the resource
 * server is introduced.
 */
package com.primeledger.config;
